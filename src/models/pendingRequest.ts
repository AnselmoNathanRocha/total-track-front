export interface GetRequest {
  id: number;
  name: string;
  email: string;
  phone: string;
  createdAt: string;
}

export interface PostRequest {
  id: number;
  name: string;
  email: string;
  phone: string;
}
